{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/main",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build with gcc",
            "stopOnEntry": false,
        }
    ]
}